[project]
name = "discord-moderation-bot"
version = "1.0.0"
description = "A professional Discord moderation bot with beautiful embeds"
requires-python = ">=3.8,<3.14"
dependencies = [
    "discord.py>=2.4.0",
    "python-dotenv==1.0.0",
]
